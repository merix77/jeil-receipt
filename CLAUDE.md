# 거래내역서 OCR 앱 (제일축산 PREMIUM)

개인용 PWA. 두 가지 기능을 담는다.

- **A. 거래내역서 OCR** — 종이 거래내역서를 촬영 → AI로 항목 추출 → 사용자 확인 → 구글시트 자동 입력
- **B. 돼지 수율표** — 이분도체 한 짝의 부위별 중량·단가 입력 → 마진금액/마진율 계산 → 구글시트 기록

사용자 1명(본인), 하루 5~10건 촬영. 앱스토어 배포 없음, 홈 화면 추가로 사용.

---

## 기술 스택 / 배포

| 레이어 | 기술 |
|---|---|
| 프론트엔드 | React + Vite (Vercel) |
| 백엔드 | Node.js + Express (Railway) |
| DB | PostgreSQL |
| 시트 연동 | Google Sheets API, 서비스 계정 인증 |

표준 응답 형식: `{ success, data, message }` / `{ success: false, error, code }`
모든 API는 `x-api-key` 헤더 필요.

---

## 구글 연동

**서비스 계정**
- 프로젝트 ID: `spiritual-hour-503102-a6`
- 이메일: `transaction-statement@spiritual-hour-503102-a6.iam.gserviceaccount.com`
- JSON 키는 로컬에만 보관, **절대 커밋 금지** — `.gitignore` 필수

**대상 스프레드시트**

| 시트 | ID | 공유 |
|---|---|---|
| 매입 (거래내역서 매입분) | `1IjsAKLfwiGti0KRZEsf_YYNoM0iKflR-J1pcNoIiWYY` | 완료 |
| 판매 (영업자간 거래내역서) | `121YEYosIYgtdzpJAuAyRLEOqRS5PKEhl-1hxRO7oJjk` | 완료 |
| 위생점검표 | `1_Ud__ZfAFF8xFhfMa-oWkfC0YSx6otrrSE5tdBGiFnY` | 완료 |
| 위생교육결과서 | `1vVdyIGuYhGELd8OZHaSNyjGXqqOOapcXAk44kkJsUWM` | 완료 |
| 제일축산 수율표 | `1WK9GIy2tJHNuECUAFJM9hFTRJwwMFDT-98n8FPz8H-U` | **확인 필요** |

모든 시트는 **`YYYY-MM` 월별 탭**에 기록(없으면 헤더와 함께 자동 생성). 수율표만 예외적으로 `요약` 탭을 추가로 둔다.

---

## DB 스키마 (영문 스네이크케이스)

```sql
CREATE TABLE receipts (
  id              SERIAL PRIMARY KEY,
  created_at      TIMESTAMP DEFAULT NOW(),
  updated_at      TIMESTAMP DEFAULT NOW(),
  is_deleted      BOOLEAN DEFAULT FALSE,
  doc_type        VARCHAR(10) NOT NULL DEFAULT 'purchase', -- 'purchase' | 'sale'
  image_url       TEXT NOT NULL,
  ocr_raw_json    JSONB,
  sheet_synced    BOOLEAN DEFAULT FALSE,
  sheet_synced_at TIMESTAMP,
  sync_error      TEXT
);

CREATE TABLE receipt_items (
  id                SERIAL PRIMARY KEY,
  created_at        TIMESTAMP DEFAULT NOW(),
  updated_at        TIMESTAMP DEFAULT NOW(),
  is_deleted        BOOLEAN DEFAULT FALSE,
  receipt_id        INTEGER NOT NULL REFERENCES receipts(id),
  trade_date        DATE NOT NULL,
  meat_type         VARCHAR(50),
  weight_kg         NUMERIC(10,2),
  origin            VARCHAR(50),
  cut_name          VARCHAR(50),
  grade             VARCHAR(20),
  slaughterhouse    VARCHAR(100),
  trace_number      VARCHAR(50),
  supplier          VARCHAR(100),
  note              TEXT,
  is_uncertain      BOOLEAN DEFAULT FALSE,
  uncertain_fields  TEXT[]
);
```

**판매분(`doc_type='sale'`)은 새 컬럼을 만들지 않고 매입 컬럼을 재사용한다.**
판매년월일 → `trade_date` / 판매처 → `supplier` / 판매부위 → `cut_name` / 판매량 → `weight_kg` / 비고 → `note`
(축종·원산지·등급·도축장명·이력번호는 판매분에서 NULL)

수율표용 테이블은 아직 설계되지 않음 — 구현 시 위 컨벤션(`created_at`/`updated_at`/`is_deleted` 포함)을 따를 것.

---

# 기능 A. 거래내역서 OCR

## OCR 엔진 (결정 변경 이력 있음 — 주의)

**현재**: Gemini API 무료 티어(`gemini-2.5-flash`)로 테스트. 정확도가 불안정하면 **Claude Sonnet 5**로 전환.

**원래 Sonnet 5로 못박았던 이유 (여전히 유효한 제약)**: `trace_number`(이력번호), `slaughterhouse`(도축장명)는 축산물이력법상 법정 기재사항이라 오인식이 법적 리스크로 이어진다. 이 때문에 Haiku 등 저가 모델도 배제했었음.

**Gemini로 테스트하는 동안에도 반드시 지킬 것**
- 확신도 낮은 필드(특히 이력번호·도축장명)는 UI에서 강하게 강조 표시하고, 사용자가 원본 사진과 대조 후 확인해야만 시트에 반영
- 실사용(매장 실제 업무) 전환 시점에 이 결정을 반드시 재검토
- Gemini 무료 티어는 데이터가 구글 모델 학습에 활용될 수 있음 — 사업자 정보 수준이라 허용했으나, 규모 커지면 유료 전환 고려

## 시트 컬럼

- **매입**: 거래년월일 / 식육·포장육의 종류 / 물량(kg) / 원산지 / 부위명칭 / 등급 / 도축장명 / 이력번호 / 매입처
- **판매**: 판매년월일 / 판매처 / 판매부위 / 판매량 / 비고

## 추출 프롬프트 (매입 기준)

판매분 프롬프트는 `services/ocrService.js`의 `SALE_PROMPT` 참조.

```
다음은 축산물 거래명세서(또는 거래내역서) 사진입니다.
아래 JSON 배열 형식으로만 응답하세요. 설명, 코드블록 표시, 다른 텍스트는 절대 포함하지 마세요.

[
  {
    "trade_date": "YYYY-MM-DD",
    "meat_type": "한우 또는 육우 등 축종",
    "weight_kg": "숫자 문자열",
    "origin": "",
    "cut_name": "안심, 등심 등 부위",
    "grade": "1++, 2 등 등급 표기",
    "slaughterhouse": "",
    "trace_number": "",
    "supplier": "공급자(매도인) 상호"
  }
]

규칙:
- 품목란이 합쳐져 있으면 축종/부위/등급을 각각의 필드로 분리
- 한 품목 = 한 행
- 값을 찾을 수 없으면 빈 문자열("")
- 읽기 어려운 글자는 추정값 뒤에 "?" 표시
- 반드시 유효한 JSON 배열만 응답
```

## API

| 엔드포인트 | 동작 |
|---|---|
| `POST /receipts` | 이미지 + `doc_type` 업로드 → 로컬 저장 → OCR → 검증(빈 결과·중복) → `receipts`+`receipt_items` 트랜잭션 저장 → 확신도 낮은 필드 flag 포함 응답 |
| `PATCH /receipts/:id/items` | 사용자 수정 반영 |
| `POST /receipts/:id/confirm` | 날짜순 정렬 → `doc_type`에 맞는 시트 월별 탭 append → 성공 시 `sheet_synced=true`, 실패 시 `sync_error` 기록. 이미 반영된 건은 재전송 차단 |
| `GET /receipts?month=YYYY-MM` | 이력 조회 (항목 포함, month 생략 가능) |
| `GET /receipts/:id/image` | 원본 사진 |
| `POST /hygiene/check` | 위생점검표 자동 O 등록 (미등록일 백필) |
| `POST /hygiene/education` | 위생교육 결과서 생성 (누락일 백필) |

---

# 기능 B. 돼지 수율표 (마진 계산)

> 대상: 돼지. 소는 부분육 단위로 매입해 구조가 달라 **추후 별도 논의 후 진행**.

## 1. 개요

정육점에 **이분도체(돼지 한 마리를 반으로 가른 상태)** 로 들여온 한 짝을 발골해, 부위별 중량과 판매단가를 입력하면 그 한 짝 기준의 **총매출액 / 마진금액 / 마진율**을 자동 계산해 시트에 기록한다.

**전제**
- 그날 들어온 돼지 전체가 아니라 **그중 한 짝만 골라 샘플 측정**한다.
- 측정은 **하루 1회**. (등록시각·마리번호 컬럼 불필요)
- 모든 중량·금액은 **이분도체(반쪽) 기준**. UI에 안내 문구 필수.

## 2. 입력 항목

**2-1. 마리 등록 (1단계)**

| 항목 | 타입 | 비고 |
|---|---|---|
| 날짜 | date | 오늘 날짜 자동 입력, 수정 가능 |
| 돈가 | number (원/kg) | 그날의 경매 돈가. **참고 기록용** |
| 총중량 | number (kg) | 이분도체 기준 |
| 총매입가 | number (원) | **직접 입력**. 협상·부대비용으로 이론값과 다를 수 있어 `돈가 × 총중량` 자동계산 안 함 |

**2-2. 부위별 입력 (2단계)** — 순서 고정

| # | 부위명 | 중량 | 단가 | 매출액 | 마진계산 |
|---|---|---|---|---|---|
| 1 | 삼겹 | O | O | 중량 × 단가 | **O** |
| 2 | 목살 | O | O | 중량 × 단가 | **O** |
| 3 | 앞다리살 | O | O | 중량 × 단가 | **O** |
| 4 | 뒷다리살 | O | O | 중량 × 단가 | **O** |
| 5 | 등심 | O | O | 중량 × 단가 | **O** |
| 6 | 항정살 | O | O | 중량 × 단가 | **O** |
| 7 | 갈매기살 | O | O | 중량 × 단가 | **O** |
| 8 | 등뼈 | **입력 없음** | **입력 없음** | **고정 5,000원** | **X** |
| 9 | 미니족 | **입력 없음** | **입력 없음** | **고정 5,000원** | **X** |

- 중량은 발골 후 **실제 저울 실측값**을 직접 입력한다. (표준수율 자동추정 없음)
- 판매단가는 시세 변동이 잦아 **매번 직접 입력**한다. (기본값 저장 안 함)
- 등뼈·미니족은 버튼 탭 한 번으로 완료 처리, 입력창을 띄우지 않는다.

## 3. 계산 로직

```
부위별 매출액 = 중량(kg) × 단가(원/kg)      // 등뼈·미니족은 고정 5,000원
총매출액     = Σ(마진계산 포함 부위의 매출액)  // 7개 부위만, 등뼈·미니족 제외
마진금액     = 총매출액 - 총매입가
마진율(%)    = 마진금액 ÷ 총매입가 × 100
```

- 등뼈·미니족의 5,000원은 **시트에 기록만 되고 총매출액에 합산하지 않는다.**
- 마진율은 소수점 첫째 자리까지 (예: 36.3%)

## 4. 시트 기록 형식

탭 2종류. **같은 트랜잭션에서 함께 기록**하고, 한쪽만 성공하면 실패 처리해 사용자에게 알린다.

### 4-1. 상세 탭 (`YYYY-MM`, 월별 자동 생성)

측정 1건 = 블록 1개. 다음 측정 시 아래에 같은 블록이 이어 붙고, **블록 사이에 빈 줄 1개**를 넣어 구분한다.

**① 공통 정보 (세로 2열)**
```
날짜          | 2026-07-29
돈가(원/kg)   | 5,800
총중량(kg)    | 42.5
총매입가(원)  | 400,000
```

**② 부위별 내역 (부위명 = 가로 헤더 / 항목 = 세로)**
```
부위명       | 삼겹    | 목살    | 앞다리살 | 뒷다리살 | 등심    | 항정살  | 갈매기살 | 등뼈  | 미니족
중량(kg)     | 9.1     | 7.8     | 5.4      | 6.6      | 6.0     | 1.6     | 0.6      | —     | —
단가(원/kg)  | 15,000  | 13,000  | 9,000    | 9,500    | 22,000  | 28,000  | 32,000   | —     | —
매출액(원)   | 136,500 | 101,400 | 48,600   | 62,700   | 132,000 | 44,800  | 19,200   | 5,000 | 5,000
마진포함     | O       | O       | O        | O        | O       | O       | O        | X     | X
```

**③ 결과 (세로 2열)**
```
총매출액(원) | 545,200
마진금액(원) | 145,200
마진율(%)    | 36.3
```

- 부위 순서 고정(삼겹 → 미니족). 입력하지 않은 부위는 중량/단가/매출액 모두 공란.

### 4-2. 요약 탭 (`요약`, 고정 1개)

측정 확정 시 **1행 append**. 필터·정렬·차트용 가로형. 첫 행 헤더 고정, 데이터는 2행부터.

`날짜 | 돈가(원/kg) | 총중량(kg) | 총매입가(원) | 총매출액(원) | 마진금액(원) | 마진율(%)`

```
2026-07-29 | 5,800 | 42.5 | 400,000 | 545,200 | 145,200 | 36.3
2026-07-30 | 5,650 | 41.8 | 385,000 | 516,800 | 131,800 | 34.2
```

## 5. UI 흐름

**홈 화면** — 기존 카드 목록(촬영하기 / 조회하기 / 위생점검 등)에 **"수율표"** 카드 1개 추가.

**1단계 · 마리 등록**
- 날짜 / 돈가 / 총중량 / 총매입가 입력 필드 4개
- 상단에 `※ 이분도체(반쪽) 기준으로 입력하세요` 안내
- `다음` 버튼

**2단계 · 부위 입력**
- 부위 버튼 9개를 3×3 그리드로 배치
- 일반 부위(1~7) 탭 → 하단에 **중량 / 단가 입력창 펼침**, 입력 완료 시 버튼에 체크 + 강조색
- 등뼈·미니족(8~9) 탭 → 입력창 없이 즉시 체크
- 하단에 **현재까지의 매출액 합계** 실시간 표시
- 입력 안 한 부위가 있어도 다음 단계 진행 가능 (선택 입력)

**3단계 · 결과 확인**
- **총매출액** 상단에 크게 / **마진금액 · 마진율** 2열 카드
- `구글시트에 반영` 버튼 → 확정 후 홈 복귀

## 6. 구현 주의사항

- 마진율 계산 시 **총매입가가 0이면 0으로 나누기 방지**
- 부위별 입력값은 확정 전까지 **로컬 상태 보관**, 뒤로 가기 시 유실 방지
- 시트 반영 성공/실패를 명확히 피드백 (기능 A와 동일 패턴)
- 고정 금액 5,000원, 부위명 리스트는 **상수로 분리** (소 수율표 추가 시 재사용 고려)

---

# UI/디자인 컨셉 — "디지털 장부"

| 용도 | 값 |
|---|---|
| 배경(종이) | `#FAF7F1` |
| 보조 배경 | `#F1ECE1` |
| 먹색 텍스트 | `#2B2724` |
| 보조 텍스트 | `#8A8071` |
| 액센트(인주 레드) | `#AF3226` |
| 구분선 | `#DDD5C7` |

- 제목: Noto Serif KR(bold) / 본문·데이터: Noto Sans KR 또는 Pretendard
- 카드 그림자보다 장부처럼 **가로줄(hairline) 구분**
- 시트 반영 완료 시 **붉은 원형 도장(印) 마크**로 표시
- **촬영 즉시 전송 금지** — 반드시 확인/수정 화면을 거친 뒤에만 전송

---

# 작업 원칙

## 코딩 전에 먼저 생각할 것
- 요청이 모호하거나 이해되지 않으면 섣불리 코드 작성을 시작하지 말고 **먼저 질문** (자가 판단·추측 금지)
- 구현 방식이 여러 개면 임의로 하나를 고르지 말고 **트레이드오프와 함께 선택지 제시**
- 실행 전에 가정한 내용을 밝히고, 더 간단한 해결책이 있으면 먼저 언급하고 의견을 물을 것

## 단순함 우선
- 문제 해결에 필수적인 최소한의 코드만 — 요청하지 않은 추가 기능·유연성은 구현하지 않음
- 일회성 코드나 단순 작업에 불필요한 Class, Interface, 디자인 패턴 등 추상화 지양
- 코드가 필요 이상으로 길면(200줄로 짠 걸 50줄로 줄일 수 있다면) 구조를 단순화해 다시 작성
- "복잡하다"고 느껴지는 구조는 즉시 단순한 구조로 변경

## 디버깅 코드는 최소한으로, 반드시 정리
- 로그가 필요하면 `console.log`/`console.error` 한두 줄만 — **새 컴포넌트나 새 상태(state)를 만들어 디버깅하지 말 것** (디버그용 컴포넌트가 렌더링 규칙을 어겨 화면 전환 기능을 깨뜨린 사례 있음)
- 디버깅용 코드는 원인 확인 후 반드시 제거하고, 제거했다고 명시적으로 알릴 것
- 디버깅 코드 추가 직후 원래 하려던 동작(화면 전환, API 호출 흐름 등)이 정상인지 한 번 더 확인 후 보고

## 기타
- 커밋/푸시는 확인 없이 자동 진행
- 영어 스크린샷 공유 시 한국어 번역 병기